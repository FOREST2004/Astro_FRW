---
import Layout from '../layouts/Layout.astro';
import PricingCalculator from '../components/PricingCalculator.jsx';
import LiveClock from '../components/LiveClock.jsx';
---

<Layout>
  <div class="font-sans text-gray-800 bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen py-12">

    <section class="text-center mb-16">
      <h1 class="text-5xl font-bold mb-4 text-gray-800">
        Astro Islands Demo
      </h1>
      <p class="text-gray-600 mb-6">
        Static HTML + React Islands
      </p>
      <a
        href="/about"
        class="inline-block bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition text-sm"
      >
        📁 File-based Routing →
      </a>
    </section>

    <p>---------------------------------------------------------------------------------------------------------------</p>

    <section class="max-w-4xl mx-auto px-4 mb-16">
      <h2 class="text-2xl font-bold mb-6 text-center">Interactive Components</h2>

      <div class="grid gap-6 md:grid-cols-2">
        <div>
          <LiveClock client:only="react" />
          <p class="text-xs text-center text-gray-500 mt-2">
            client:only
          </p>
        </div>

        <p>- - - - - - - - - - - - - - - - </p>

        <div>
          <PricingCalculator client:load />
          <p class="text-xs text-center text-gray-500 mt-2">
            client:load (hydrate ngay khi load)
          </p>
        </div>
      </div>
    </section>

    <p>---------------------------------------------------------------------------------------------------------------</p>

    <section class="max-w-4xl mx-auto px-4 mb-16">
      <h2 class="text-2xl font-bold mb-6 text-center">Lợi ích</h2>
      <div class="grid gap-6 md:grid-cols-3">
        <div class="bg-white rounded-xl shadow-md p-6 text-center">
          <div class="text-4xl mb-2">🚀</div>
          <h3 class="font-bold mb-2">Performance</h3>
          <p class="text-sm text-gray-600">Ít JS hơn</p>
        </div>
        <div class="bg-white rounded-xl shadow-md p-6 text-center">
          <div class="text-4xl mb-2">🎯</div>
          <h3 class="font-bold mb-2">SEO</h3>
          <p class="text-sm text-gray-600">HTML có sẵn</p>
        </div>
        <div class="bg-white rounded-xl shadow-md p-6 text-center">
          <div class="text-4xl mb-2">⚡</div>
          <h3 class="font-bold mb-2">Fast</h3>
          <p class="text-sm text-gray-600">Tải nhanh</p>
        </div>
      </div>
    </section>

    <footer class="text-center py-8 text-gray-500 text-sm">
      Astro + React Islands
    </footer>

  </div>
</Layout>
